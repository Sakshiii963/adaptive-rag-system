# Evaluation Plan

RAGAS evaluation is deferred until retrieval and generation exist. The current suite verifies API health/error contracts, semantic chunk metadata, duplicate hash lookup, PDF extraction, SQLite job metadata, Chroma persistence after reopening, BM25 construction, and multi-file upload/job polling.
