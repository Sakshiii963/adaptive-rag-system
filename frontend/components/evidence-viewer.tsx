import type { Evidence } from "../lib/types";

export function EvidenceViewer({ evidence }: { evidence: Evidence[] }) {
  return <section className="surface rounded-2xl border p-4"><div className="mb-4 flex items-center justify-between"><h3 className="text-sm font-semibold">Evidence viewer</h3><span className="muted text-xs">{evidence.length} passages</span></div>{evidence.length === 0 ? <p className="muted rounded-xl border border-dashed p-4 text-center text-xs">No evidence was retained for this answer.</p> : <div className="scrollbar-thin max-h-[420px] space-y-3 overflow-auto">{evidence.map((item) => <article key={item.chunk_id} className="rounded-xl border p-3"><div className="mb-2 flex flex-wrap items-center gap-2 text-[11px]"><span className="rounded bg-teal-100 px-2 py-0.5 font-semibold text-teal-800 dark:bg-teal-950 dark:text-teal-200">[{item.rank}]</span><span className="muted">{item.filename} · p. {item.page_number}</span><span className="muted">{item.chunk_id}</span></div><p className="text-sm leading-6">{item.text}</p><div className="muted mt-2 flex gap-3 text-[11px]"><span>retrieval {item.retrieval_normalized_score.toFixed(2)}</span><span>reranker {item.normalized_reranker_score.toFixed(2)}</span></div></article>)}</div>}</section>;
}
