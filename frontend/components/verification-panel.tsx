import type { VerificationReport } from "../lib/types";

export function VerificationPanel({ report }: { report: VerificationReport }) {
  return <section className="surface rounded-2xl border p-4"><h3 className="mb-4 text-sm font-semibold">Verification panel</h3><div className="mb-4 grid grid-cols-2 gap-2 text-center text-xs"><div className="rounded-xl bg-emerald-50 p-3 text-emerald-800 dark:bg-emerald-950/40 dark:text-emerald-200"><p className="text-lg font-bold">{report.claims.filter((claim) => claim.supported).length}</p><p>supported claims</p></div><div className="rounded-xl bg-rose-50 p-3 text-rose-800 dark:bg-rose-950/40 dark:text-rose-200"><p className="text-lg font-bold">{report.unsupported_claim_ids.length}</p><p>unsupported claims</p></div></div><div className="space-y-2">{report.claims.map((claim) => <div key={claim.claim_id} className="rounded-lg border p-2"><div className="flex gap-2 text-xs"><span>{claim.supported ? "✓" : "!"}</span><p>{claim.text}</p></div><p className="muted mt-1 text-[10px]">{claim.reason}</p></div>)}</div>{report.unsupported_citations.length > 0 && <p className="mt-3 text-xs text-rose-700 dark:text-rose-300">Rejected citations: {report.unsupported_citations.map((item) => `[${item}]`).join(", ")}</p>}</section>;
}
